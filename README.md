# multi-agent-investigation-rag
This is a multi-agent platform that allows a user to reasearch on a topic based on the input of one or more LLM model.

Deploy to `fraserver01` with one command from Windows:

```bat
dp_remote.bat
```
